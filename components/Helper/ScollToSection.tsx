import React from 'react'

const ScollToSection = () => {
    const scrollToSection = () =>{
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        })
    }

  return (
    <div>ScollToSection</div>
  )
}

export default ScollToSection